import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    active: {
        backgroundColor: "rgba(51, 102, 255, .08)",
        borderLeftWidth: 4,
        borderLeftColor: '#3366FF'
    }
});